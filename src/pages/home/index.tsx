import clsx from "clsx";

export default function HomePage() {
  return (
    <div
      className={clsx("w-full h-full h-full")}
      style={{ backgroundColor: "#fff" }}
    >
      this is home page
    </div>
  );
  return <div style={{ backgroundColor: "#fff" }}>this is home page</div>;
}
